def disambiguateAcronym(acronym, sentence):
    # currently just returns the input acronym. It should output its expansion instead (e.g. "United States" given "US")
    expandedAcronym = "expanded" + acronym # Placeholder added by Leo to make things work for now
    return expandedAcronym